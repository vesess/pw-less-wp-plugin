# Database Token Encryption (for tokens stored in database)
# Generate a random 32-character string for DB_KEY
PWLESS_DB_KEY="xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
# Generate a random 16-character string for DB_IV
PWLESS_DB_IV="xxxxxxxxxxxxxxxx"

# URL Token Encryption (for tokens in magic links)
# Generate a different random 32-character string for URL_KEY
PWLESS_URL_KEY="yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy"
# Generate a random 16-character string for URL_IV
PWLESS_URL_IV="yyyyyyyyyyyyyyyy"

# User ID Encryption (for user IDs in URLs)
# Generate another random 32-character string for UID_KEY
PWLESS_UID_KEY="zzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzz"
# Generate a random 16-character string for UID_IV
PWLESS_UID_IV="zzzzzzzzzzzzzzzz"

# Rate Limiting Settings (optional)
# Maximum number of login attempts within the time window
# PWLESS_MAX_ATTEMPTS=5
# Time window in seconds for rate limiting (default: 300 seconds = 5 minutes)
# PWLESS_ATTEMPT_WINDOW=300

# To generate secure random strings, you can use:
# Linux/Mac:
#   openssl rand -base64 24 | cut -c1-32  # For 32-char keys
#   openssl rand -base64 12 | cut -c1-16  # For 16-char IVs
# 
# Windows (PowerShell):
#   -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | ForEach-Object {[char]$_})  # For 32-char keys
#   -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 16 | ForEach-Object {[char]$_})  # For 16-char IVs
